% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/networks_storage.R
\name{biblio_store}
\alias{biblio_store}
\title{Store a bibliometric project using Arrow or DuckDB}
\usage{
biblio_store(x, path, engine = c("arrow", "duckdb"), overwrite = FALSE)
}
\arguments{
\item{x}{A \code{biblio_project}.}

\item{path}{Directory (Arrow) or database file (DuckDB).}

\item{engine}{Storage engine.}

\item{overwrite}{Replace existing output; when \code{FALSE} (default), an existing \code{path} raises an error.}
}
\value{
Normalized output path, invisibly.
}
\description{
Store a bibliometric project using Arrow or DuckDB
}
\examples{
\donttest{
x <- as_biblio_project(example_biblio())
if (requireNamespace("arrow", quietly = TRUE)) {
  p <- tempfile()
  biblio_store(x, p, "arrow")
  biblio_load(p, "arrow")
}
if (requireNamespace("duckdb", quietly = TRUE) &&
    requireNamespace("DBI", quietly = TRUE)) {
  p <- tempfile(fileext = ".duckdb")
  biblio_store(x, p, "duckdb")
  biblio_load(p, "duckdb")
}
if (requireNamespace("arrow", quietly = TRUE)) {
  p <- tempfile()
  biblio_store(x, p, "arrow", overwrite = TRUE)
}
}
}
