% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/python.R
\name{biblium_compare_means}
\alias{biblium_compare_means}
\title{Compare group means with Biblium 2.16}
\usage{
biblium_compare_means(
  x,
  metric = c("citations", "velocity", "year"),
  groups,
  alpha = 0.05,
  python = NULL
)
}
\arguments{
\item{x}{A \code{biblio_project}.}

\item{metric}{\code{"citations"}, \code{"velocity"} or \code{"year"}.}

\item{groups}{A single categorical grouping (vector, factor or function of
\code{x$works}); multi-column membership matrices are refused.}

\item{alpha}{Significance level.}

\item{python}{Optional Python executable.}
}
\value{
A \code{biblio_means_comparison} list with engine, descriptives, tests,
post-hoc, recommended test and interpretation.
}
\description{
Compares a numeric work metric across groups with automatic test selection
(t/Welch/Mann-Whitney for two groups; ANOVA/Welch-ANOVA/Kruskal-Wallis plus
post-hoc for three or more), normality and homogeneity checks, and effect
sizes (Cohen's d / eta squared).
}
\examples{
\dontrun{
x <- as_biblio_project(example_biblio())
biblium_compare_means(x,groups=ifelse(x$works$year<2022,"early","late"))
biblium_compare_means(x,metric="velocity",groups=x$works$source)
}
}
