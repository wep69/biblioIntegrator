% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/python.R
\name{biblium_diversity}
\alias{biblium_diversity}
\title{Diversity indices with Biblium 2.16}
\usage{
biblium_diversity(
  x,
  entity = c("keyword", "author", "source"),
  level = c("overall", "temporal", "group"),
  groups = NULL,
  min_items_per_year = 5L,
  python = NULL
)
}
\arguments{
\item{x}{A \code{biblio_project}.}

\item{entity}{\code{"keyword"}, \code{"author"} or \code{"source"}.}

\item{level}{\code{"overall"}, \code{"temporal"} or \code{"group"}.}

\item{groups}{Group definition accepted by \code{\link[=form_groups]{form_groups()}}; required when \code{level="group"}.}

\item{min_items_per_year}{Minimum works per year in \code{"temporal"} level.}

\item{python}{Optional Python executable.}
}
\value{
A \code{biblio_diversity} list with engine, indices and summary/trend.
}
\description{
Shannon, Simpson and Gini indices over authors, keywords or sources, for the
whole corpus (\code{"overall"}), per year (\code{"temporal"}) or per group (\code{"group"}).
}
\examples{
\dontrun{
x <- as_biblio_project(example_biblio())
biblium_diversity(x)
biblium_diversity(x,entity="author",level="temporal",min_items_per_year=1)
biblium_diversity(x,level="group",groups=ifelse(x$works$year<2022,"early","late"))
}
}
