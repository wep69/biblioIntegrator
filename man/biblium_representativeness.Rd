% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/python.R
\name{biblium_representativeness}
\alias{biblium_representativeness}
\title{Representativeness against the OpenAlex benchmark with Biblium 2.16}
\usage{
biblium_representativeness(
  x,
  category = "year",
  threshold = 1,
  reference = NULL,
  use_cache = TRUE,
  python = NULL
)
}
\arguments{
\item{x}{A \code{biblio_project}.}

\item{category}{Currently only \code{"year"} (works carry year; country, doctype
and OA status need per-work enrichment).}

\item{threshold}{Absolute pp difference classified as over/under-represented.}

\item{reference}{Optional data frame with \code{Year} and \code{Count} used instead of
the live OpenAlex benchmark (offline testing).}

\item{use_cache}{Logical: reuse the on-disk OpenAlex reference.}

\item{python}{Optional Python executable.}
}
\value{
A \code{biblio_representation} list with engine, table and cache flag.
}
\description{
Compares the corpus year distribution to global OpenAlex counts through
percentage-point differences (\code{compute_relative_representation()}). The
reference is cached on disk; only a cache miss reaches the network.
}
\examples{
\dontrun{
x <- as_biblio_project(example_biblio())
biblium_representativeness(x)
}
}
